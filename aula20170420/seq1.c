#include <stdio.h>

int main(){
   float lado;
   float area;
   printf("entre com o lado do quadrado: ");
   scanf("%f", &lado);
   area= lado*lado;
   printf("A area do quadrado e: %f", area);
   return 0;
}
